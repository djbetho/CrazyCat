<%= form_with(model: @comeent) do |form| %>
  <% if comeent.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comeent.errors.count, "error") %> prohibited this comeent from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>
 

  <div>
    <%= form.hidden_field :poost_id, value: @poost %>
 
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
